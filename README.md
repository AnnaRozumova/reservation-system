# Reservation System

This project is a web application featuring a reservation system for events. It is being developed for a non-profit Buddhist organization to facilitate the organization of Buddhist events, such as meditation retreats.

The backend logic is implemented in Python, using the Django framework for views, models, and URL routing. SQLite is used as the database to store and manage data. The user interface and interactions are built with HTML, CSS, Bootstrap, and JavaScript.
